# ArgusGate Agent 工作约束

本文件适用于在本仓库中工作的所有 AI Agent。项目行为、阶段范围和工程执行方式分别以 `docs/design.md`、`docs/v0.1_roadmap.md` 和 `docs/engineering.md` 为准。

## 授权与范围

- Agent 只能执行用户明确要求的工作，以及完成该工作不可缺少的最小配套修改。
- 未经用户明确允许，Agent 不得自行添加功能、接口、配置项、依赖、目录或面向未来的抽象。
- 未经用户明确允许，Agent 不得新增、删除、提前、推迟、重排或替换路线图任务，也不得改变当前 Phase、版本边界或项目开发方向。
- 发现范围外需求、潜在改进或路线调整机会时，Agent 只能说明依据、影响和建议；必须取得用户明确授权后才能实施或修改路线文档。
- 修复测试、文档或构建问题时，不得借机扩大功能范围。若最小修复会改变外部行为、公共接口、配置格式或路线安排，应先停止并请求用户决定。

## 变更要求

- 开始修改前核对当前 Phase、工作区状态和相关设计约束。
- 保留用户已有的未提交改动，不得擅自覆盖、回退或重写。
- 行为变化必须配套测试，并同步实际受影响的文档；不得把尚未实现的能力标记为完成。
- 交付时说明完成内容、验证结果、未执行检查和仍未完成的路线项。
